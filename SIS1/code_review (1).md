# 📊 CODE REVIEW: Machine Learning - Student Independent Tasks

## 📋 Общая информация

**Файлы на проверку:**
- `SIS1.ipynb` - Содержит задания уровней A и B
- `LevelC.ipynb` - Содержит только задание уровня C

**Курс:** CSCI3234 — Machine Learning  
**Лекции:** 1-3

---

## 🎯 КРАТКАЯ ОЦЕНКА

### SIS1.ipynb
**Общая оценка: 7.5/10** ⭐⭐⭐⭐

**Сильные стороны:**
✅ Хорошо структурированный код  
✅ Используются правильные библиотеки  
✅ Есть визуализации  
✅ Присутствуют комментарии

**Что нужно улучшить:**
⚠️ Недостаточно markdown-объяснений между кодом  
⚠️ Не все графики подписаны  
⚠️ Некоторые расчёты можно оптимизировать

### LevelC.ipynb
**Общая оценка: 8/10** ⭐⭐⭐⭐

**Сильные стороны:**
✅ Отличные markdown-объяснения  
✅ Правильное применение методов сэмплирования  
✅ Хороший анализ результатов  
✅ Сравнение разных подходов

**Что нужно улучшить:**
⚠️ Можно добавить больше визуализаций  
⚠️ Жёстко заданный путь к файлу  

---

## 📚 ОБЪЯСНЕНИЕ ДЛЯ ЧАЙНИКА

### Что вообще делают эти ноутбуки?

Представьте, что вы — детектив, который изучает улики (данные). Эти ноутбуки — ваши инструменты для:
1. **Изучения данных** (что у нас есть?)
2. **Очистки данных** (убрать мусор)
3. **Анализа данных** (найти закономерности)
4. **Подготовки данных** для машинного обучения

---

## 🔍 ДЕТАЛЬНЫЙ РАЗБОР SIS1.ipynb

### LEVEL A - Простой уровень

#### A1. Типы данных и атрибуты

**Что происходит:**
```python
import numpy as np
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

df = pd.read_csv("winequality-red.csv")
```

**Объяснение для чайника:**
1. **import** - это как достать инструменты из ящика:
   - `numpy` - калькулятор для массивов чисел
   - `pandas` - таблица Excel на стероидах
   - `matplotlib` и `seaborn` - художники для графиков

2. **df = pd.read_csv(...)** - загружаем данные из CSV файла в "таблицу" (DataFrame)
   - CSV - это файл, где данные разделены запятыми (Comma-Separated Values)
   - DataFrame - это таблица с строками и столбцами

**Датасет:** Данные о красном вине (качество вина и его характеристики)

**Типы атрибутов:**
- **Nominal** (номинальный) - просто названия, без порядка (например, цвет: красный, синий)
- **Ordinal** (порядковый) - есть порядок (например, размер: маленький, средний, большой)
- **Discrete** (дискретный) - целые числа (например, количество комнат: 1, 2, 3)
- **Continuous** (непрерывный) - любые числа с дробями (например, температура: 36.6, 36.7)

#### A2. Описательная статистика

**Что происходит:**
```python
mean_values = df[num_cols].mean()
median_values = df[num_cols].median()
mode_values = df[num_cols].mode().iloc[0]
```

**Объяснение для чайника:**

📊 **Статистические показатели:**

1. **Mean (среднее)** = сумма всех чисел ÷ количество чисел
   - Пример: [1, 2, 3, 4, 5] → среднее = 15÷5 = 3

2. **Median (медиана)** = число посередине когда все числа выстроены по порядку
   - Пример: [1, 2, **3**, 4, 5] → медиана = 3
   - Если чисел чётное количество, берём среднее двух средних

3. **Mode (мода)** = самое часто встречающееся число
   - Пример: [1, 2, 2, 2, 3, 4] → мода = 2

4. **Range (размах)** = максимум - минимум
   - Показывает, насколько разбросаны данные

5. **Variance (дисперсия)** = насколько числа отклоняются от среднего
   - Чем больше, тем более "разбросаны" данные

6. **Standard Deviation (стандартное отклонение)** = √дисперсия
   - Более понятная мера разброса данных

7. **IQR (межквартильный размах)** = разница между 75% и 25% данных
   - Помогает найти выбросы (странные значения)

**Boxplot (ящик с усами):**
```
    ┌─── максимум (без выбросов)
    │
    ├─── 75% (Q3)
  ┌─┼─┐
  │ │ │ ← ящик
  └─┼─┘
    ├─── 25% (Q1)
    │
    └─── минимум (без выбросов)
    
    * ← выбросы (outliers)
```

#### A3. Обработка пропущенных данных

**Что происходит:**
```python
# Удаляем 10% случайных значений
missing_mask = np.random.rand(*df[num_cols].shape) < 0.1
df_missing = df[num_cols].copy()
df_missing[missing_mask] = np.nan
```

**Объяснение для чайника:**

Представьте, что у вас анкета, но некоторые люди не заполнили все вопросы. Что делать?

**3 стратегии:**

1. **Удаление (deletion)** - просто выбрасываем строки с пропусками
   - ❌ Проблема: теряем информацию
   - ✅ Плюс: просто и быстро

2. **Заполнение средним (mean imputation)**
   ```
   Было: [5, 7, ?, 9, 11]
   Стало: [5, 7, 8, 9, 11]  (? заменили на среднее = 8)
   ```

3. **Заполнение медианой (median imputation)**
   ```
   Было: [5, 7, ?, 9, 100]
   Стало: [5, 7, 7, 9, 100]  (? заменили на медиану = 7)
   ```
   - Лучше для данных с выбросами (100 не повлияет на медиану)

**Сравнение:**
```python
print(f"Original mean: {df[num_cols].mean()['fixed acidity']:.2f}")
print(f"Mean imputation: {mean_imputed.mean()['fixed acidity']:.2f}")
print(f"Median imputation: {median_imputed.mean()['fixed acidity']:.2f}")
```
Это показывает, как каждый метод повлиял на данные.

---

### LEVEL B - Средний уровень

#### B1. Нормализация и Стандартизация

**Объяснение для чайника:**

Представьте две характеристики:
- Рост человека: 150-200 см
- Вес человека: 50-100 кг

Разные шкалы! Нужно привести к одному масштабу.

**1. Min-Max Normalization (нормализация)**
```
Формула: (x - min) / (max - min)
Результат: все значения от 0 до 1

Пример:
Было: [10, 20, 30, 40, 50]
Стало: [0, 0.25, 0.5, 0.75, 1]
```

**2. Z-score Standardization (стандартизация)**
```
Формула: (x - среднее) / стандартное_отклонение
Результат: среднее = 0, большинство значений от -3 до +3

Пример:
Было: [10, 20, 30, 40, 50]
Среднее = 30, σ = 15.8
Стало: [-1.26, -0.63, 0, 0.63, 1.26]
```

**Когда что использовать:**
- **Min-Max**: когда нужны значения в диапазоне [0, 1], для нейронных сетей
- **Z-score**: когда есть выбросы, для большинства ML алгоритмов

#### B2. Создание признаков и дискретизация

**Объяснение для чайника:**

**1. Feature Creation (создание новых признаков)**
```python
df['total_acidity'] = df['fixed acidity'] + df['volatile acidity']
```
Это как создать новую колонку "полная кислотность" из двух существующих.

**Зачем?**
- Комбинация признаков может быть полезнее, чем отдельные
- Пример: BMI = вес / (рост²) - это новый признак из двух старых

**2. Discretization (дискретизация)**
```
Превращаем непрерывные числа в категории:

Alcohol:          Category:
8.5  ───────────> Low
10.2 ───────────> Medium  
13.8 ───────────> High
```

**Методы дискретизации:**

a) **Equal Width (равная ширина)** - делим диапазон на равные части
```
[0-10] [10-20] [20-30]
Каждый интервал одинаковой ширины (10)
```

b) **Equal Frequency (равная частота)** - в каждой группе примерно одинаковое количество элементов
```
Группа 1: [1,2,3,4,5]     - 5 элементов
Группа 2: [6,7,8,9,10]    - 5 элементов
Группа 3: [11,12,13,14,15] - 5 элементов
```

c) **K-means** - умный алгоритм группировки похожих значений
```
Находит "центры" групп и относит каждое значение к ближайшему центру
```

---

## 🎓 ДЕТАЛЬНЫЙ РАЗБОР LevelC.ipynb

### LEVEL C - Продвинутый уровень: Sampling (Выборка)

**Датасет:** NBA Basketball (65,698 игр)

**Проблема:** Слишком много данных! Компьютер будет долго обрабатывать.

**Решение:** Взять ПРЕДСТАВИТЕЛЬНУЮ выборку (sample) - меньше данных, но которые отражают всю картину.

---

#### Подготовка данных

**Что происходит:**
```python
num_cols = [
    "pts_home",      # очки домашней команды
    "pts_away",      # очки гостевой команды
    "reb_home",      # подборы домашней команды
    "reb_away",      # подборы гостевой команды
    "ast_home",      # передачи домашней команды
    "ast_away",      # передачи гостевой команды
    "fg_pct_home",   # процент попаданий домашней команды
    "fg_pct_away"    # процент попаданий гостевой команды
]
```

Выбираем только числовые колонки для анализа.

---

#### 1. Simple Random Sampling (Простая случайная выборка)

**Код:**
```python
random_sample = df.sample(frac=0.1, random_state=42)
```

**Объяснение для чайника:**

Представьте мешок с 1000 шариками разных цветов. Вы закрываете глаза и достаёте 100 шариков наугад.

- **frac=0.1** означает "возьми 10% данных"
- **random_state=42** - это "магическое число" для воспроизводимости (чтобы каждый раз получать одинаковую выборку)

**Плюсы:**
✅ Просто и быстро  
✅ Честно и беспристрастно

**Минусы:**
❌ Можем случайно взять неравномерно  
❌ Пример: в мешке 900 красных и 100 синих шариков, а мы случайно взяли 95 красных и 5 синих

---

#### 2. Stratified Sampling (Стратифицированная выборка)

**Код:**
```python
strat_sample, _ = train_test_split(
    df_valid,
    test_size=0.9,      # берём 10% (остальные 90% выбрасываем)
    stratify=df_valid["season_id"],  # сохраняем пропорции сезонов
    random_state=42
)
```

**Объяснение для чайника:**

Теперь представьте, что шарики разделены по ящикам (сезонам). В каждом ящике разное количество шариков.

**Стратифицированная выборка** = из каждого ящика берём ПРОПОРЦИОНАЛЬНО:
```
Ящик 2015: 1000 шариков → берём 10% = 100 шариков
Ящик 2016: 500 шариков  → берём 10% = 50 шариков
Ящик 2017: 1500 шариков → берём 10% = 150 шариков
```

**Зачем?**
✅ Гарантируем, что каждый сезон представлен правильно  
✅ Избегаем ситуации, когда случайно взяли только игры из одного сезона

**Проверка:**
```python
print("Original distribution:")
print(df["season_id"].value_counts(normalize=True).head())

print("\nStratified sample distribution:")
print(strat_sample["season_id"].value_counts(normalize=True).head())
```
Это показывает, что пропорции сохранены!

---

#### 3. Cluster Sampling (Кластерная выборка)

**Код:**
```python
teams = df["team_id_home"].unique()  # получаем все команды
selected_teams = np.random.choice(teams, size=5, replace=False)  # выбираем 5 команд
cluster_sample = df[df["team_id_home"].isin(selected_teams) | 
                    df["team_id_away"].isin(selected_teams)]
```

**Объяснение для чайника:**

Представьте школу с 30 классами. Вместо того, чтобы опрашивать по несколько учеников из каждого класса, мы:
1. Выбираем 5 ЦЕЛЫХ классов наугад
2. Опрашиваем ВСЕХ учеников из этих 5 классов

**В нашем случае:**
- "Классы" = команды NBA
- Выбираем 5 случайных команд
- Берём ВСЕ игры, где эти команды участвовали

**Плюсы:**
✅ Удобно, когда данные естественным образом группируются  
✅ Дешевле (меньше "перемещений" между группами)

**Минусы:**
❌ Может быть менее представительно  
❌ Если случайно выберем только сильные или только слабые команды

---

#### Сравнение методов выборки

**Что делает код:**
```python
for col in num_cols:
    print(f"\n{col}:")
    print(f"  Full dataset mean: {df[col].mean():.2f}")
    print(f"  Random sample mean: {random_sample[col].mean():.2f}")
    print(f"  Stratified sample mean: {strat_sample[col].mean():.2f}")
    print(f"  Cluster sample mean: {cluster_sample[col].mean():.2f}")
```

**Объяснение:**
Это как проверить, насколько хорошо наша маленькая выборка представляет всю картину:

```
Полный датасет:          среднее_очков = 105.50
Случайная выборка:       среднее_очков = 105.48  ✅ Близко!
Стратифицированная:      среднее_очков = 105.51  ✅ Очень близко!
Кластерная:              среднее_очков = 103.20  ⚠️ Отличается
```

**Что это значит:**
- Стратифицированная выборка лучше всего сохранила характеристики оригинальных данных
- Кластерная выборка дала немного другие результаты (потому что мы взяли только 5 команд)

---

## 🎯 ПРАКТИЧЕСКИЕ РЕКОМЕНДАЦИИ

### Для улучшения SIS1.ipynb:

1. **Добавить больше markdown-объяснений:**
```markdown
## Интерпретация результатов

После применения mean imputation мы заметили, что...
Это означает, что...
```

2. **Улучшить визуализации:**
```python
plt.figure(figsize=(12, 6))
plt.title("Comparison of Imputation Methods", fontsize=16)
plt.xlabel("Feature", fontsize=12)
plt.ylabel("Mean Value", fontsize=12)
# ... остальной код
```

3. **Добавить проверки:**
```python
# Проверка на пропущенные значения
print(f"Missing values before: {df.isnull().sum().sum()}")
print(f"Missing values after: {df_imputed.isnull().sum().sum()}")
```

### Для улучшения LevelC.ipynb:

1. **Использовать относительные пути:**
```python
# Вместо:
df = pd.read_csv("C:/Users/...")

# Лучше:
df = pd.read_csv("nba_games.csv")  # файл в той же папке
```

2. **Добавить визуальное сравнение:**
```python
fig, axes = plt.subplots(2, 2, figsize=(15, 10))
df['pts_home'].hist(ax=axes[0,0], bins=50)
axes[0,0].set_title('Full Dataset')

random_sample['pts_home'].hist(ax=axes[0,1], bins=50)
axes[0,1].set_title('Random Sample')
# ... и так далее
```

3. **Добавить статистические тесты:**
```python
from scipy import stats

# Проверка, насколько выборка репрезентативна
t_stat, p_value = stats.ttest_ind(df['pts_home'], strat_sample['pts_home'])
print(f"P-value: {p_value:.4f}")
if p_value > 0.05:
    print("Выборка статистически не отличается от генеральной совокупности ✅")
```

---

## 📊 ФИНАЛЬНАЯ ОЦЕНКА

### SIS1.ipynb: 7.5/10

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность кода | 9/10 | Код работает правильно |
| Структура | 8/10 | Хорошая организация |
| Комментарии | 6/10 | Есть, но можно больше |
| Визуализации | 7/10 | Базовые графики есть |
| Интерпретация | 7/10 | Нужно больше объяснений |

### LevelC.ipynb: 8/10

| Критерий | Оценка | Комментарий |
|----------|--------|-------------|
| Корректность кода | 9/10 | Отлично реализовано |
| Структура | 9/10 | Очень хорошая организация |
| Комментарии | 8/10 | Отличные markdown-объяснения |
| Визуализации | 6/10 | Можно добавить графики |
| Интерпретация | 9/10 | Хороший анализ результатов |

---

## 🚀 КЛЮЧЕВЫЕ ВЫВОДЫ

### Что вы ДЕЛАЕТЕ ХОРОШО:

1. ✅ Правильно используете библиотеки
2. ✅ Код структурирован логично
3. ✅ Применяете правильные методы для каждой задачи
4. ✅ В LevelC отличные объяснения

### Что НУЖНО УЛУЧШИТЬ:

1. ⚠️ Больше markdown-ячеек с объяснениями ЧТО и ПОЧЕМУ делаете
2. ⚠️ Все графики должны иметь заголовки и подписи осей
3. ⚠️ Добавить интерпретацию результатов после каждого анализа
4. ⚠️ Использовать относительные пути к файлам
5. ⚠️ Добавить больше визуализаций для сравнения методов

---

## 📚 ПОЛЕЗНЫЕ ТЕРМИНЫ (Глоссарий)

- **DataFrame** - таблица данных в pandas (как Excel таблица)
- **Feature** - признак, характеристика (столбец в таблице)
- **Sample** - выборка (подмножество данных)
- **Outlier** - выброс (странное значение, которое сильно отличается от остальных)
- **Imputation** - заполнение пропущенных значений
- **Normalization** - приведение данных к диапазону [0, 1]
- **Standardization** - приведение данных к среднему=0 и σ=1
- **Stratification** - разделение на слои/группы
- **Cluster** - группа, кластер
- **Representative** - представительный (хорошо отражает всю совокупность)

---

**Удачи в обучении! 🎓**
